<h1>My Tasks</h1>

<% @tasks.each do |index| %>
<h3><%= link_to (index.completed ? "[x]" : "[ ]") + " " + index.title, show_path(index) %></h3>
 <!-- link_to index.title, show_path(index) -->
<% end %>

<h4><%= link_to "Add New Task", new_path %></h4>
